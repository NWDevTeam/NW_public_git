IRQ_unite_shiites_category = {
	allowed = {
		original_tag = IRQ
		NOT = {
			has_global_flag = THE_MOTHER_OF_ORDER_FLAG
			has_global_flag = WORLD_OF_SPLIT_FLAG
		}
	}
	visible = {
		has_completed_focus = IRQ_unity_of_islam_and_communism
		NOT = { is_in_faction_with = PER }
	}
	icon = saf_anti_colonialist_crusade
	priority = 100
}

IRQ_middle_eastern_racing_competitions_category = {
	allowed = {
		original_tag = IRQ
		NOT = {
			has_global_flag = THE_MOTHER_OF_ORDER_FLAG
			has_global_flag = WORLD_OF_SPLIT_FLAG
		}
	}
	visible = {
		has_completed_focus = IRQ_hold_race_competition
	}
	icon = saf_anti_colonialist_crusade
	priority = 100
}
