nw_drop_focus_tree = {
	icon = log_service_categories
	priority = 10000
	allowed = {
		is_ai = no
		NOT = { tag = SAI }
	}
	available = {
		hidden_trigger = {
			has_global_flag = { flag = nw_drop_focus_tree value > 0 }
		}
	}
	visible = {
		is_ai = no
		has_global_flag = { flag = nw_drop_focus_tree value > 0 }
	}
}

log_service_categories = {
	icon = log_service_categories
	allowed = {
		NOT = { tag = SAI }
	}
	visible = {
		has_global_flag = log_service
	}
	priority = -100
}

achievements_categories = { # TODO а где?
	icon = log_service_categories
	allowed = {
		always = no
	}
	priority = -100
}

debug_decisions_2 = {
	icon = generic_research
	allowed = {
		NOT = { tag = SAI }
	}
	visible = {
		is_ai = no
		OR = {
			has_global_flag = debug_decisions_2_visible
			is_debug = yes
		}
	}
	priority = -991
}