KAS_war_preparations = { # TODO убрать
	icon = GFX_decision_category_infiltration
	allowed = {
		original_tag = KAS
		NOT = {
			has_global_flag = THE_MOTHER_OF_ORDER_FLAG
			has_global_flag = WORLD_OF_SPLIT_FLAG
		}
	}
	visible = {
		OR = {
		    has_war_with = SIK
			has_war_with = TNG
		}
	}
	priority = 200
}